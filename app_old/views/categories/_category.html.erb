<div class="product_categories">
  <% if category.image? %>
    <div>
      <%= link_to image_tag(category.image.url(:index)), products_path(:category => category.id) %>
    </div>
  <% end %>
  <h2><%= link_to h(category.name), products_path(:category => category.id) %></h2>
</div>
