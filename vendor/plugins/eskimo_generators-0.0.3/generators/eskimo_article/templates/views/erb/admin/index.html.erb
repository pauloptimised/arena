<%% title "<%= plural_title %>" %>

<p><%%= link_to "New <%= singular_title %>", new_admin_<%= singular_name %>_path %></p>
 
<%% form_for @search do |f| %>
  <p>
    <%%= f.text_field :headline_or_summary_or_main_content_like %>
    <%%= f.submit "Search" %>
  </p>
<%% end %>

<%%= admin_table({:columns => [
  "headline", 
  ["Summary", lambda{|x| (x.summary.length > 50) ? "#{x.summary[0..50]}..." : x.summary}, "summary"], 
  "date"], :list => @<%= plural_name %>, :images => true}) %>
